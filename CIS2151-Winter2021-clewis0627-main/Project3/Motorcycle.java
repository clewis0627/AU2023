package Project3;

public class Motorcycle extends Cart {

    public Motorcycle(int maxSpeed, int acceleration, int breakSpeed) {
        super(maxSpeed + 3, acceleration +3, breakSpeed +3);
        
    }

    while(turnGo == true) {
        if(track.wipeout() == true) {
            stop();
        }
    }

    public Item cartItem(Item item, "") {
        this.item = getItem();
        return this.item;
    }
    
}
